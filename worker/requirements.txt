fastapi==0.115.5
uvicorn[standard]==0.32.1
httpx>=0.26,<0.28
boto3==1.35.76          # Cloudflare R2 (S3-compatible)
supabase==2.10.0
python-dotenv==1.0.1
yt-dlp>=2026.8.19          # floor, not pin: YouTube breaks old versions within months
ffmpeg-python==0.2.0
numpy==2.1.3
opencv-python-headless==4.10.0.84
opencv-python<5                 # insightface pulls this in unpinned; 5.x drops CascadeClassifier and shadows headless
insightface==1.0.1              # face detection + recognition embeddings (portrait pipeline)
onnxruntime>=1.20,<2            # insightface backend
spacy==3.8.2
replicate==0.34.2
openai==1.57.2          # OpenRouter uses OpenAI-compatible API
pydantic==2.10.2
aiofiles==24.1.0
psutil>=5.9                     # worker RSS logging (pipeline/memlog.py)
tenacity==9.0.0
python-multipart==0.0.18
Pillow>=10.0                    # subtitle rendering (PIL Image / ImageDraw / ImageFont)
fonttools[woff]>=4.0            # WOFF2 → TTF conversion for Google Fonts
pilmoji==2.0.5                  # colour-emoji rendering in the text sticker (PIL has no emoji fallback)
