# Root .env.example — see web/.env.local.example and worker/.env.example for per-service config
# Generate a strong shared secret for Next.js ↔ Worker auth:
# python3 -c "import secrets; print(secrets.token_urlsafe(32))"
WORKER_SECRET=replace-me
